#include "main.h"
/**
 * _isupper - check if c is uppercase
 * @c: is an integer
 * copyright © 2022 baccrie
 * Return: Always (1) or (0)
 */

int _isupper(int c)
{
	if (c <= 'Z' && c >= 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	/* Copyright © 2022 baccrie */
}

#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of any
 * interger n
 * @n: n is an integer
 * Copyright © 2022 baccrie
 *
 * Return: integer
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	/* Copyright © 2022 baccrie */
}
